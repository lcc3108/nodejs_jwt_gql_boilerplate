{
  "name": "test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && yarn install",
    "build": "cd backend && yarn build",
    "prebuild": "yarn install:all",
    "deploy": "terraform plan && terraform apply --auto-approve"
  }
}
